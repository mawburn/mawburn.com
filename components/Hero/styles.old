@import '../../styles/vars';

$color-light: #f0f0f0;
$color-shadow: rgba(0, 0, 0, 0.75);

.hero {
  display: flex;
  flex-direction: column;

  @media #{$bpTabletUp} {
    flex-direction: row;
  }
}

.tagline {
  display: flex;
  justify-content: center;
  align-items: center;
  width: 300px;
  min-height: 30px;
  text-align: center;
  color: $color-light;
  font-family: 'Baloo 2', sans-serif;
  font-size: 1.75rem;
  text-shadow: 0 2px 10px $color-shadow;

  span {
    margin: 0 1ch;
  }

  @media #{$bpTabletUp} {
    min-height: 50px;
  }
}

.photo {
  margin: 1rem auto 0;
  width: 150px;
  height: 150px;
  border: 2px solid rgba(#fff, 0.42);
  border-radius: 50%;
  overflow: hidden;
  box-shadow: 5px 5px 15px 5px #000;
}

.heHimText {
  text-align: center;
  color: $color-light;
  padding-top: 0.5rem;
}
